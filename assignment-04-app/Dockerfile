# ---- Build stage ----
FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

# ---- Run stage (Nginx) ----
FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html

# Replace main nginx config (ensures "server" is under http {})
COPY nginx.conf /etc/nginx/nginx.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
